2026-03-29 Task 7 blocker: mirrored source shows no preservation-safe non-interactive refresh path. `bunx oh-my-opencode install --no-tui` requires explicit provider flags (`src/cli/index.ts:26-33`, `src/cli/install.ts:112-149`) and regenerates model assignments from those flags (`src/cli/model-fallback.ts:125-223`) before merging/writing config (`src/cli/config-manager.ts:324-343`). This preserves extra keys structurally via deep merge, but still rewrites generated `agents`/`categories` model choices, so any retry must redefine providers and risks config drift.
