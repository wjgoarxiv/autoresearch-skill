Task 3 - OpenCode update method and target version
Date: 2026-03-29

Current state
- Current OpenCode version: 1.3.3
- Current shell-resolved binary path: /Users/woojin/.opencode/bin/opencode
- Binary path realpath: /Users/woojin/.opencode/bin/opencode
- Binary file type clue: regular file in ~/.opencode/bin (not a symlink)

Official docs basis
- Official CLI docs (`https://opencode.ai/docs/cli#upgrade`) document `opencode upgrade [target]` and the `--method` flag with supported values `curl`, `npm`, `pnpm`, `bun`, `brew`.
- Official installer (`https://opencode.ai/install`) installs the standalone binary into `$HOME/.opencode/bin` and supports `--version <version>` for pinned installs.
- Latest stable release at execution time from `https://api.github.com/repos/anomalyco/opencode/releases/latest`: `v1.3.3` (published `2026-03-26T15:05:47Z`, `prerelease=false`, `draft=false`).

Install-method reasoning
- The live `opencode` command resolves to `/Users/woojin/.opencode/bin/opencode`.
- The official installer script writes to the same directory: `$HOME/.opencode/bin`.
- The resolved binary is a regular file, not a Homebrew symlink or a package-manager shim.
- Local package-manager checks did not show a managed `opencode` install on this machine: `brew list --formula opencode` failed, and global `npm`, `pnpm`, and `bun` package checks did not report `opencode`.
- Therefore the strongest supported interpretation is: this machine is using the standalone installer path, so the matching OpenCode upgrade method is `curl`.

Chosen update command for the next task
- Exact command: `opencode upgrade v1.3.3 --method curl`
- Why this command: it uses the official self-upgrade surface, makes the install method explicit instead of relying on auto-detection, and pins the target to the latest stable version available at execution time so Task 4 has no ambiguity.

Target version policy for this run
- Policy: latest stable available at execution time unless a stronger reason to pin appears.
- Result for this run: latest stable is `v1.3.3`, so the explicit target is pinned to `v1.3.3`.
- Note: the currently installed version is already `1.3.3`, so Task 4 may result in a no-op/reinstall confirmation rather than a version change. That is acceptable because the plan allows either a changed version or a match to the chosen explicit target version.

Fallback and rollback basis
- Preferred execution path: `opencode upgrade v1.3.3 --method curl`
- Fallback source if the self-upgrade path is unavailable or the binary becomes unusable: the official installer script at `https://opencode.ai/install`, using its documented version pinning support.
- Binary rollback command basis: `curl -fsSL https://opencode.ai/install | bash -s -- --version 1.3.3 --no-modify-path`
- Config rollback basis: restore from the verified pre-update backup directory recorded in Task 2: `/Users/woojin/.config/opencode/backups/pre-update-20260329-011819`
- Verified backup contents relevant to rollback: `opencode.json`, `oh-my-opencode.json`, `package.json`, and `switch-config.sh` are present in that backup directory.

Decision
- Use the official OpenCode self-upgrade command with explicit `curl` method and explicit `v1.3.3` target.
