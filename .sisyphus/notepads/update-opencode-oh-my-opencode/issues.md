- 2026-03-29T01:15:54 Task 1 correction: preflight plugin array was incorrectly reported as [] because the parser read `plugins` instead of live config key `plugin`; fixed by sourcing `plugin` (with `plugins` fallback) and updating only the plugin-array line in task-1-preflight.txt.
- Task 7 blocker: exact command `bunx oh-my-opencode install` exited without a TTY and printed `Error: Interactive installer requires a TTY. Use --non-interactive or set environment variables directly.` No alternate flags or auth/provider flows were used. Preservation checks remained `True`, so no restore was needed.
- Task 7 retry confirmed the TTY blocker is unavoidable in a preservation-safe way: local docs/source only document `--no-tui`, and it requires explicit provider flags, so no corrected non-interactive command was run.
